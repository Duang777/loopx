# Host-plane only. LoopX owns durable authority; this package keeps only
# process-local coordination state.
- insert:
    - id: loopx-service
      name: dsh-loopx-plugin/service

    - id: loopx-command
      name: dsh-loopx-plugin/command

    - id: loopx-tools
      name: dsh-loopx-plugin/tools

    - id: loopx-driver
      name: dsh-loopx-plugin/driver
